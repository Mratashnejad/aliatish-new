// import React from 'react';
// import NavbarClient from '@/components/navbarClient';
// import { Locale } from '@/i18n.config';
// import { getDictionary } from '@/lib/dictionary';

// interface NavbarServerProps {
//     lang: Locale;
// }

// export default async function NavbarServer({ lang }: NavbarServerProps) {
//     const { navigation } = await getDictionary(lang);
    
//     console.log('Navigation:', navigation); // Add this line to check the output

//     return <NavbarClient lang={lang} navigation={navigation} />;
// }
